<%= form_with(model: pet_breed) do |form| %>
  <% if pet_breed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pet_breed.errors.count, "error") %> prohibited this pet_breed from being saved:</h2>

      <ul>
        <% pet_breed.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :breed_id %>
    <%= form.text_field :breed_id %>
  </div>

  <div class="field">
    <%= form.label :pet_id %>
    <%= form.text_field :pet_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: list_of_breed) do |form| %>
  <% if list_of_breed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(list_of_breed.errors.count, "error") %> prohibited this list_of_breed from being saved:</h2>

      <ul>
        <% list_of_breed.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :breed_name %>
    <%= form.text_field :breed_name %>
  </div>

  <div class="field">
    <%= form.label :species %>
    <%= form.text_field :species %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
